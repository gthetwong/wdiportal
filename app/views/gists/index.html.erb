<div class="gist center small">

<h2>Gists</h2>

<%= button_to "New gist", new_gist_path, method: "get", class: "btn btn-warning" %> <br>

<table>

	<% @gists.each do |f| %>
	
		<tr>
	    	<td>
	      		<%= link_to f.title, "#{f.url}", :target => "_blank" %>
	      	</td>
	      	<td>
	      		Instructor
			</td>
			<td>
	      		<%= button_to "Edit", edit_gist_path(f), :method => "get", :class => "btn btn-default" %>
			</td>
    	</tr>
 
	<% end %>

</table><br>

</div>